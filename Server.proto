syntax = "proto3";

package user;

message Request {
  string username = 1;
}

message Response {
  bool success = 1;
}

service RegisterService {
  rpc register_user (Request) returns (Response) {}
}